# Input Documents

Place `.docx` and `.pdf` files here for analysis. This repository includes sample Document360 knowledge-base exports:

- `Microsoft Word documents (.docx)/` - 5 Word files
- `PDF documents (.PDF)/` - 5 matching PDF files

Run from project root:

```bash
python run.py
```
